"""
正文写作流程技能：完整的小说正文创作工作流。
从大纲检查、角色状态同步、记忆规则查询，到章节撰写、润色、字数检查的端到端流程。
"""
from core.skills.registry import Skill


def create_skill() -> Skill:
    return Skill(
        name="draft_writer",
        display_name="正文写作流程",
        description="完整的小说正文创作工作流。从大纲检查、角色状态同步、记忆规则查询，到章节撰写、润色、字数检查的端到端流程。",
        keywords=["写正文", "写章节", "开始写", "写第", "续写", "补写", "重写正文", "初稿", "草稿"],
        tools=["read_file", "write_file", "patch_file", "glob", "grep", "query_memory"],
        wing="创作",
        content="""# 核心指令：正文写作专家

## 触发条件（主动判断）
当你发现以下情况时，**主动激活此技能**：
*   用户要求写某一章或某一段正文
*   用户说"开始写"、"写正文"、"写第X章"
*   用户要求续写、补写、或重写正文内容

---

## 工作流总览

P1 创作准备 → P2 撰写正文 → P3 润色优化 → P4 收尾检查

**此技能被激活时，已自动解锁 outline、character、memory 工具类别，无需再调用 search_tools。**

---

## P1：创作准备（必须先做，不可跳过）

### 1.1 同步大纲状态

**必须调用** outline 工具确认当前章节位置：

1. 读取大纲文件（章节大纲/）— 查看卷结构和章节列表
2. 定位当前要写的是第几章
3. 查看该章节已有的事件规划

**关键判断**（写正文前必须确认）：
- **如果该章节还没有事件规划** → 先创建事件，**不要直接写正文**
- **如果大纲中该章节的事件为空或过于简略** → 先补充大纲事件内容，明确本章要发生什么
- **如果已有事件** → 记录事件标题、涉及角色、情绪基调、计划字数

> 禁止行为：大纲里没有本章剧情就动笔写。没有事件规划 = 没有创作方向。

### 1.2 读取上一章正文（剧情衔接检查）

**必须调用** read_file 读取上一章正文文件：

1. 通过 glob(正文/*) 找到上一章的文件路径
2. read_file(上一章路径) — 确认上一章结尾的剧情状态

**衔接检查清单**：
- 上一章结尾的钩子是什么？本章开头必须承接这个钩子
- 上一章结束时角色的状态？（位置、伤势、情绪、关系变化）
- 上一章埋下的新伏笔？本章是否需要呼应或推进？
- 上一章承诺给读者但还没交付的内容？（如"下章揭晓"类预告）

> 禁止行为：不读上一章就直接写，导致剧情断层、角色状态矛盾。

### 1.3 查询未收尾伏笔

**必须调用** foreshadowing 工具获取所有未完结的伏笔列表：

**判断**：
- 有已到期该回收的伏笔？→ 本章安排回收
- 有近期要推进的伏笔？→ 本章给线索或暗示
- 没有可用伏笔？→ 考虑在本章埋一个新伏笔
- 发现某个伏笔的计划回收章不合理？→ 调整 plannedChapter

**操作**：
- 如需回收/推进某个伏笔 → 查看伏笔详情
- 如需修改伏笔属性 → 更新伏笔
- 如需在本章埋新伏笔 → 在写正文时自然植入

### 1.4 同步角色状态

**必须调用** character 工具查询本章涉及的所有角色：

1. 查询角色档案（角色/）— 查看角色静态设定
2. 查询角色关系网络 — 查看角色当前关系
3. 如果角色有动态状态 → 记录当前状态

**关键检查**：
- 角色在本章开始时的状态是什么？（伤势、情绪、关系变化）
- 角色在本章结束时的目标状态是什么？
- 状态变化是否有合理的因果链？

### 1.5 查询写作规则

**按需调用** memory 工具查询长期规则：

1. query_memory(keywords: ["写作规则", "语气", "禁忌"]) — 查询是否有用户设定的特殊写作规则
2. query_memory(keywords: ["世界观", "力量体系"]) — 查询世界观相关约束

**不要查询故事内容或角色信息** — 这些通过 read_file 获取，不存记忆宫殿。

---

## P2：撰写正文

### 2.1 加载写作子技能（按需激活）

正文写作是综合能力，**根据本章内容类型，必须加载对应子技能**：

| 本章内容类型 | 必须加载的子技能 | 激活命令 |
|------------|----------------|---------|
| 有角色对话（任何对话场景） | 对话写作 | activate_skill("dialogue_writing") |
| 有打斗/战斗/比武 | 打斗场景 | activate_skill("combat_scenes") |
| 有情绪爆发/心理转折 | 情绪渲染 | activate_skill("emotion_rendering") |
| 有新场景/环境需要描写 | 场景描写 | activate_skill("scene_description") |
| 从细纲扩写成正文 | 正文扩写 | activate_skill("draft_expander") |
| 润色、去AI味、文风改造 | 文本润色 | activate_skill("text_polish") |

**判断规则**：
- 本章只要有对话 → **必须**加载「对话写作」
- 本章只要有打斗 → **必须**加载「打斗场景」
- 情绪、场景、扩写、润色按需加载
- **可以同时加载多个技能**，没有冲突

> 禁止行为：明明要写对话却不加载「对话写作」，导致对话平淡无味；明明要写打斗却不加载「打斗场景」，导致战斗写成流水账。

### 2.2 创作约束确认

动笔前用双轴快速自检（无需调用 deep_thinking，简单任务直接判断）：

| 检查项 | 标准 |
|-------|------|
| 驱动力 | 本章靠什么让读者翻页？悬念？情感？发现？ |
| 目的性 | 删掉本章，读者少获得什么？ |
| 情绪曲线 | 从什么情绪值开始，到什么情绪值结束？ |
| 钩子 | 章末是否有明确的阅读期待？ |

### 2.3 写作规范

**文件命名**：正文/卷[X]_章[X]_[章节名].md

**Frontmatter 模板**：

    ---
    summarys: []
    tags: ["正文"]
    characters: ["角色A", "角色B"]
    ---

**字数要求**：
- 每章 3000-5000 字
- 写完后用 grep 或 read_file 检查字数

**节奏控制**：
- 前 20% 必须有即时冲突或强吸引力
- 中间 50% 推进核心事件，至少一个转折点
- 后 30% 高潮 + 章末钩子

### 2.4 工具调用顺序

撰写正文时只使用基础文件工具（write_file/patch_file/read_file），不需要 outline/character/memory 工具。

---

## P3：润色优化（撰写后必须执行）

### 3.1 连贯性检查

1. **人物一致性**：本章角色行为是否符合其档案设定？
2. **情节连贯**：与上一章的衔接是否自然？状态变化是否有交代？
3. **节奏控制**：是否有连续多段无冲突的"平铺直叙"？

### 3.2 去除 AI 痕迹

**必须加载「文本润色」技能**：

进入润色阶段时，**必须先调用** activate_skill("text_polish")，按技能指引执行完整的去AI化检测和改写流程。

**禁止行为**：凭感觉手动修改几句就结束润色，漏掉系统性检测。

**技能加载后必须检查并修改**：
- ❌ 过度修饰形容词（"璀璨"、"瑰丽"、"绚烂"）→ 删减或用具体细节替代
- ❌ 抽象陈述（"心中涌起复杂的情感"）→ 改为具体动作/对话
- ❌ 四字格律堆砌（"心潮澎湃、热血沸腾"）→ 打破节奏，用口语化表达
- ❌ 书面语套话 → 增加个性化对话
- ❌ 长句单调 → 长短句交替
- ❌ 笼统描述 → 具体的视觉/听觉/嗅觉细节

### 3.3 钩子检查

章末必须留下至少一种钩子：
- 悬念钩子：未解之谜
- 危机钩子：角色陷入险境
- 转折钩子：预期被打破
- 期待钩子：新的目标/诱惑

---

## P4：收尾检查

### 4.1 更新大纲与伏笔

**必须** 同步进度：

1. 如果本章事件已经存在 → 更新事件状态为"已完成"
2. 如果本章是新事件 → 将本章内容转化为结构化事件
3. 如果本章埋了新伏笔或推进/回收了伏笔 → 确保同步到 Timeline

### 4.2 更新角色状态

**必须** 更新角色动态：

1. 分析本章内容，自动更新角色状态
2. 检查是否有新关系产生或旧关系变化

### 4.3 标记 TODO 完成

如果当前章节有对应的 TODO，标记为已完成。

---

## 反模式（正文写作常见错误）

| 反模式 | 表现 | 纠正方法 |
|--------|------|---------|
| **跳过准备直接写** | 不看大纲、不查角色状态就动笔 | 严格执行 P1 流程 |
| **脱离大纲放飞** | 写的剧情和大纲事件对不上 | 写前确认事件，写后更新事件 |
| **角色状态断层** | 上一章重伤，这章满血无解释 | 查角色档案，状态变化要有交代 |
| **AI 味浓** | 形容词堆砌、抽象陈述、套话 | P3 润色清单逐条检查 |
| **无钩子收尾** | 章节结束得像段落结束 | 强制要求章末钩子 |
| **字数不足** | 低于 3000 字 | 扩充细节、增加场景、深化冲突 |
| **工具人配角** | 配角只服务于主角，没有主体性 | 给配角至少一个独立动机 |

---

## 与其他 Skill 的关系

- **深度思考方法论**：复杂章节（多线交汇、重大转折）写前可调用 deep_thinking 做约束分析
- **场景描写**：需要强化环境氛围时 activate_skill("scene_description")
- **对话写作**：对话占比过高或缺乏个性时 activate_skill("dialogue_writing")
- **战斗场景**：涉及战斗时 activate_skill("combat_scenes")
- **情感渲染**：情感高潮章节 activate_skill("emotion_rendering")
- **文本润色**：完成初稿后可 activate_skill("text_polish") 进行深度润色
""",
    )
