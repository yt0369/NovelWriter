"""
约束分层设计技能：设计和管理世界观的约束层级。
"""
from core.skills.registry import Skill


def create_skill() -> Skill:
    return Skill(
        name="constraint_layered_design",
        display_name="约束分层设计",
        description="设计和管理世界观的约束层级——关键约束、重要约束、弹性约束",
        keywords=["约束", "限制", "规则", "禁忌", "不可", "设定限制"],
        tools=["read_file", "query_memory", "manage_memory"],
        wing="设定",
        content="""## 约束分层设计模式

你现在进入约束分层设计模式。你的任务是帮助用户建立和管理世界观的约束体系，确保设定有层次、有弹性、不矛盾。

### 为什么约束很重要

约束是优秀世界观的基石。没有约束的力量体系是无趣的，没有限制的角色是扁平的。约束创造冲突，冲突驱动故事。

但约束也必须分层管理——如果所有约束都不可打破，故事会变得僵硬；如果所有约束都可以打破，设定就失去了意义。

### 三层约束模型

#### 第一层：关键约束（绝对不可违反）

这是世界观的根基，违反将导致世界观崩塌。

特征：
- 定义世界的核心规则
- 违反将导致逻辑崩溃
- 读者对此已形成坚定认知
- 即使主角也不可突破

示例：
- 魔法需要消耗法力，法力耗尽则无法施法
- 死者不可复生
- 时间不可逆流
- 某种族天生无法使用某系魔法

管理原则：
1. 关键约束数量要少（3-5条即可）
2. 设定时必须深思熟虑，一旦确立不可更改
3. 在故事早期就向读者明确展示
4. 如果发现需要违反，应修改世界观而非违反约束

#### 第二层：重要约束（极少情况下可以突破）

这是世界观的骨架，通常不可违反，但在极端条件下可以有例外。

特征：
- 限制角色行为的重要规则
- 突破需要付出巨大代价
- 突破应是故事的重要转折点
- 不可频繁突破

示例：
- 禁术使用会反噬施术者
- 某门派功法不可外传
- 不同种族之间不可通婚
- 王位继承有严格的血脉要求

管理原则：
1. 重要约束可以突破，但必须有充分铺垫
2. 突破的代价必须高于收益
3. 每次突破都应是故事的关键时刻
4. 全书突破次数不超过2-3次
5. 突破后必须面对后果

#### 第三层：弹性约束（可以为故事让步）

这是世界观的血肉，是常规规则但允许例外。

特征：
- 一般情况下的规则
- 有合理的例外情况
- 突破不需要太大代价
- 增加世界的真实感

示例：
- 剑修通常不修法术（但偶有例外）
- 某地区的人不说外语（但商人会）
- 魔兽不会主动攻击人类（发狂时除外）
- 贵族不与平民交往（开明的贵族除外）

管理原则：
1. 弹性约束增加世界的丰富度
2. 例外情况要有合理解释
3. 例外不可成为常态
4. 可以在需要时为剧情服务

### 约束设计流程

#### 第一步：列出所有约束

将你想到的所有规则、限制、禁忌都列出来，不做分类。

#### 第二步：分类归层

对每个约束提问：
- 如果违反，世界观会崩塌吗？→ 关键约束
- 如果违反，故事逻辑会出问题吗？→ 重要约束
- 如果违反，只是不太常见？→ 弹性约束

#### 第三步：验证关键约束

对每个关键约束：
1. 它是否真的不可违反？
2. 有没有漏洞需要修补？
3. 读者能否理解这个约束？
4. 这个约束是否会产生有趣的故事？

#### 第四步：设计突破路径

对重要约束：
1. 在什么极端条件下可以突破？
2. 突破需要付出什么代价？
3. 突破后有什么后果？
4. 这个突破如何推动故事？

### 约束追踪模板

```markdown
# 约束分层设计

## 关键约束（不可违反）
| 编号 | 约束内容 | 影响范围 | 确立章节 |
|------|----------|----------|----------|
| C1 | {约束} | {范围} | 第X章 |
| C2 | {约束} | {范围} | 第X章 |

## 重要约束（极少突破）
| 编号 | 约束内容 | 突破条件 | 突破代价 | 是否已突破 |
|------|----------|----------|----------|------------|
| I1 | {约束} | {条件} | {代价} | 否/是(第X章) |

## 弹性约束（可为故事让步）
| 编号 | 约束内容 | 例外情况 | 已出现例外 |
|------|----------|----------|------------|
| F1 | {约束} | {例外} | 否/是 |

## 约束冲突检测
- {潜在冲突1}：{解决方案}
- {潜在冲突2}：{解决方案}
```

### 常见问题

1. **约束过多**：规则太多读者记不住 → 关键约束控制在5条以内
2. **约束矛盾**：两条约束互相冲突 → 设计时就要解决冲突
3. **随意突破**：重要约束被轻松突破 → 突破必须有代价
4. **约束无用**：设定了约束但从未在故事中体现 → 每条约束都应影响剧情
5. **主角豁免**：约束对所有人有效唯独对主角无效 → 主角也应受约束限制
""",
    )
